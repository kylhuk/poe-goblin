# Issues

- 2026-03-15: `task()` worker refused the broad Task 1 regression bundle as "multiple tasks" even though it is one plan item; execute Task 1 as several file-scoped delegations and only mark the plan task complete after the full six-file pytest command passes.
- 2026-03-15: Final verification review blocker: current package-local CLI evidence does not match the plan's required verification commands. `poe_trade/evidence/qa/seed*.json` and `poe_trade/evidence/summary.md` only prove `scan plan --limit 5` plus `research backtest --strategy high_dim_jewels`, while the plan's parity verification requires `scan plan --limit 20` and `research backtest --strategy bulk_essence` for the same seeded snapshot; no package-local artifact records that non-journal backtest CLI command.
- 2026-03-15: Package-local parity artifacts are internally inconsistent: `poe_trade/evidence/qa/seed.json` and `poe_trade/evidence/qa/seed-journal.json` record journal/backtest run IDs and summary text that do not match their paired CLI proof files (`seed-research-backtest.txt`, `seed-journal-research-backtest.txt`), so the current runtime/CLI evidence cannot be treated as one reproducible verification bundle.
- 2026-03-15: Final verification quality blocker: `poe_trade/strategy/backtest.py` never loads active journal keys, so journal-gated backtests always resolve to `no_opportunities` even when matching `poe_trade.journal_positions` state exists. Probe command: `python3 - <<'PY' ... backtest.run_backtest(...) ... print('queried_journal_positions=', any('journal_positions' in q for q in client.queries))` returned `queried_journal_positions= False` and `no_opportunities= True`.
