- mirrored `poe_trade/sql/strategy/bulk_essence/candidate.sql` into the backtest-live-scanner-parity tree to keep the strategy contract synced and validated the change with the requested pytest command.
- added `poe_trade/sql/strategy/bulk_fossils/candidate.sql` so the strategy now has a local candidate definition; pytest run still fails because the registry tests assert no candidate files exist yet.
- added `poe_trade/sql/strategy/scarab_reroll/candidate.sql` with the canonical reroll contract and reran the provided pytest subset; the registry assertion that no candidate SQL exists now triggers a failure.
- ported `poe_trade/sql/strategy/fossil_scarcity/candidate.sql` into the backtest tree; the pytest subset still fails because the registry asserts no candidate SQL exists, which is expected while the file is present.
- extended the parity harness by asserting the candidate SQL files expose the expected scanner aliases, the scanner discovery/data tests cover the direct-column contract, backtest tests now prove `no_opportunities` vs `no_data`, and CLI tests lock the populated recommendation/status output—runtime still relies on the JSON-based scanner path, so this regression harness documents the future expectations until the cutover lands.
- added an enabled discover SQL contract test for non-journal packs; the guard currently catches the unaliased `SELECT *` queries, so the provided pytest subset now fails until those discover files define the expected column aliases.

- defined explicit fragment set, map/logbook, and cx candidate contracts from the gold reference backtests and updated the enabled set discovers to emit typed columns, `item_or_market_key` bridge aliases, and `listing_count AS sample_count` so the guard can validate the contract; no automated verification was run, and the broader registry suite still depends on the other Wave 2 discover bridges.
- added canonical listing candidate contracts for seven listing strategies with explicit scanner aliases, stable semantic keys (`category:base_type:price_currency`), listing mart evidence columns, and rewrote enabled listing discover bridges (`cluster_basic`, `flask_basic`) to explicit `sample_count` alias contracts without `SELECT *`.
- aligned the four bulk-premium candidate SQL files so they explicitly emit `time_bucket AS time_bucket`, `realm`, the derived profit/roi/confidence metrics, canonical sample counts, and the descriptive scanner/evidence columns that mirror the backtest formulas and keep `item_or_market_key` stable.
- rewrote the enabled bulk discover bridges to project the same alias set (including `bulk_listing_count AS sample_count` and non-empty `why_it_fired`/`buy_plan`/`exit_plan`) and noted that `.venv/bin/pytest tests/unit/test_strategy_registry.py tests/unit/test_strategy_policy.py -q` still fails because the global file guard sees other strategy SQL files that have yet to adopt the required aliases.

- tightened cx_market_making candidate aliasing (including `sample_count AS listing_count`) and expanded the fragment/map discover bridges with the scanner-required text/metrics, then ran `.venv/bin/pytest tests/unit/test_strategy_registry.py tests/unit/test_strategy_policy.py tests/unit/test_strategy_scanner.py -q`; the only remaining failure is the hard-coded canonical candidate set in `tests/unit/test_strategy_registry.py`.
- updated registry canonical candidate set to include every pack and reran .venv/bin/pytest tests/unit/test_strategy_registry.py tests/unit/test_strategy_policy.py tests/unit/test_strategy_scanner.py -q to confirm the discovery guard now aligns with the full rollout.
- switched scanner runtime to load canonical candidate SQL via registry loader, enforce minima from direct source columns (profit/roi/confidence plus sample-count compatibility keys), and prefer source item_or_market_key with cityHash fallback while keeping source_row_json/evidence_snapshot; remaining blocker for full backtest parity is Task 8 wiring the same candidate/policy model into backtest execution.
- cut over `poe_trade/strategy/backtest.py` to `load_candidate_sql(pack)` plus shared policy evaluation, so `no_data`/`no_opportunities` now come from explicit canonical source vs eligible counts instead of source-table regex parsing; with scanner and backtest both on the canonical candidate/policy path, the runtime side of Task 6 is now satisfied in both paths.
- seeded package-local QA evidence at `poe_trade/evidence/qa/` (seed + seed-journal + state JSON) and repointed evidence indexing to `poe_trade/evidence/`; remaining final-wave blocker is external to this task: parity refresh/reporting still needs Task 10 aggregation updates.

- refreshed the evidence bundle summary to narrate the canonical candidate/journal parity workflow and status snapshots now stored under poe_trade/evidence so Task 9 proof stays non-placeholder.
- moved scanner/backtest onto shared `policy_from_pack` + `candidate_from_source_row` helpers so league/minima/cooldown parity now runs through `evaluate_candidates` with candidate `time_bucket` replay and legacy hashed-key cooldown cutover; remaining package-local blocker is still Task 10 QA/evidence aggregation in `poe_trade/qa_contract.py` and `poe_trade/evidence_bundle.py`.
- package-local QA seeding now writes source mart fixtures (`gold_bulk_premium_hour`, `gold_listing_ref_hour`), executes real `run_backtest` + `run_scan_once`, and records paired scanner/backtest key parity plus a journal-gated `no_opportunities` summary in `poe_trade/evidence/qa/seed*.json`, which `poe_trade/evidence/summary.md` now surfaces directly.
- routed journal-only packs through the shared policy evaluator by fetching `poe_trade.journal_positions` keys before gating, so packs without active positions still run but emit no recommendations; verified with `.venv/bin/pytest tests/unit/test_strategy_scanner.py -q` and `.venv/bin/pytest tests/unit/test_strategy_scanner.py -q -k journal`.
- ensured backtest replay groups same-timestamp candidates before rolling the cooldown history so `evaluate_candidates()` still dedupes/ranks snapshot duplicates exactly as the shared policy path; matching unit coverage now proves the duplicate winner/count semantics.
- shifted the package-local journal-gated QA seed to `high_dim_jewels`, seeded `gold_listing_ref_hour` with a `cluster_jewel` listing, and reran `.venv/bin/python -m poe_trade.qa_contract seed --output poe_trade/evidence/qa/seed-journal.json` + `.venv/bin/python -m poe_trade.evidence_bundle` so the evidence bundle now shows the journal-state summary for the new scenario.
- package-local seeding now captures real `poe_trade.cli scan plan` and `poe_trade.cli research backtest --strategy high_dim_jewels` stdout/stderr under `poe_trade/evidence/qa/cli/`, and summary inventory wording now references those proof artifacts instead of the stale `advanced_rare_finish` text.
- tuned the journal-gated `high_dim_jewels` coin seed (larger depth/profit) so the QA backtest now clears minima and fails only because of `requires_journal`, then re-ran the seed/evidence bundle to capture the new journal-state summary.
- aligned the non-journal QA proof captures with the plan commands (`scan plan --league Mirage --limit 20` plus `research backtest --strategy bulk_essence --league Mirage --days 14`) while leaving the journal seed on the high_dim_jewels track.
