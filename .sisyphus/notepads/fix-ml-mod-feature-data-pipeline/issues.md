- 2026-03-18: Running `.venv/bin/python -m pytest tests/unit/test_ml_tuning.py` fails in existing train-loop tests because `SimpleNamespace` test clients lack `.execute()` while `train_loop()` calls `_dataset_row_count()`; this is unrelated to mod-feature population changes but appeared during related-suite verification.
- 2026-03-18: Post-rebuild Mirage coverage still shows very low canonical mod key density (`LIKE '%tier%'` = `1745` rows, `0.02%` of `11467342`) despite non-empty check (`mod_features_json != '{}'`) reporting `10690356` rows (`93.22%`); investigate why many rows are non-`{}` but not carrying `{ModName}_tier` / `{ModName}_roll` keys.
- 2026-03-18: Matcher expansion moved `%tier%` from `1745` to `1762` Mirage rows (`+17`) with `%roll%` also `1762`, while total rows are `11643912`; this delta is not material and remains ~`0.02%` canonical coverage.
