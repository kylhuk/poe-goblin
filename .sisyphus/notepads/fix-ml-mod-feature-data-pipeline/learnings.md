- 2026-03-18: `build_dataset()` now calls `_populate_item_mod_features_from_tokens()` after rebuilding `ml_item_mod_tokens_v1`, so canonical dataset flow populates `poe_trade.ml_item_mod_features_v1` from tokenized mods instead of relying on migration `0041` placeholder `{}` rows.
- 2026-03-18: Feature payload generation in `poe_trade/ml/workflows.py` emits sparse keys using `{ModName}_tier` and `{ModName}_roll`, with non-empty JSON verified by `.venv/bin/python -c "from poe_trade.ml.workflows import _mod_features_from_tokens ..."`.
- 2026-03-18: `_populate_item_mod_features_from_tokens(..., league=...)` now performs league-scoped delete (`ALTER TABLE ... DELETE WHERE league = ...`) instead of table-wide truncate, so rerunning Mirage refresh does not wipe other leagues.
- 2026-03-18: Rebuild evidence command run inside container: `docker compose exec ml_trainer poe-ml dataset build --league Mirage --as-of "$(date -u +%Y-%m-%dT%H:%M:%SZ)" --output-table poe_trade.ml_price_dataset_v1` returned `rows_written: 11467342` for `Mirage`.
- 2026-03-18: Mirage verification query `SELECT count() FROM poe_trade.ml_price_dataset_v1 WHERE league = 'Mirage'` returned `11467342`, confirming rows are present again after rebuild.
- 2026-03-18: Mirage coverage checks returned `10690356` rows (`93.22%`) for `mod_features_json != '{}'` and `1745` rows (`0.02%`) for both `mod_features_json LIKE '%tier%'` and `mod_features_json LIKE '%roll%'`; sampled tier payloads now show canonical keys like `{"Intelligence_tier": 8, "Intelligence_roll": 0.04, ...}` instead of timestamp-shaped strings.
- 2026-03-18: `_mod_features_from_tokens()` now normalizes quoted token text, handles observed phrase variants (`increased movement speed`, `to all elemental resistances`, `to maximum life`, `to chaos resistance`), and parses `adds X to Y <type> damage` ranges so canonical `{ModName}_tier`/`{ModName}_roll` keys are emitted for a materially broader set of real token-table rows.
- 2026-03-18: Post-matcher-expansion Mirage rebuild command `docker compose exec ml_trainer poe-ml dataset build --league Mirage --as-of 2026-03-18T17:52:50Z` completed successfully with `rows_written: 11643912`.
- 2026-03-18: Verification counts after rebuild: total Mirage rows `11643912`; `mod_features_json != '{}'` `10854375`; `mod_features_json LIKE '%tier%'` `1762`; `mod_features_json LIKE '%roll%'` `1762`.
- 2026-03-18: `%tier%` coverage improved from baseline `1745` to `1762` (`+17`, `+0.97%` relative increase), and sampled payloads contain canonical keys such as `{"MaximumEnergyShield_tier": 5, "MaximumEnergyShield_roll": 0.05}`.
- 2026-03-18: `_fit_route_bundle_from_aggregates(...)` gradient-boosting regressors now use a lower learning rate with more estimators plus subsampling (`subsample` + `max_features='sqrt'`), which increases effective capacity for the 52-dimension mod-feature space while keeping route/category logic unchanged.
- 2026-03-18: Focused verification for the tuning-only change passed: `.venv/bin/pytest tests/unit/test_ml_tuning.py -k fit_route_bundle_uses_tuned_gradient_boosting_params_with_mod_features` and `.venv/bin/pytest tests/unit/test_ml_mod_feature_population.py`.
- 2026-03-18: Route-aware tuning trial kept the stronger default bundle for fallback-style routes but regularized `structured_boosted` and `sparse_retrieval`; focused tests passed with `.venv/bin/pytest tests/unit/test_ml_tuning.py -k "fit_route_bundle_uses_tuned_gradient_boosting_params_with_mod_features or fit_route_bundle_uses_more_regularized_params_for_sparse_retrieval"`.
- 2026-03-18: Bounded Mirage eval with route-aware tuning (`POE_CLICKHOUSE_URL=http://127.0.0.1:8123 .venv/bin/poe-ml train-loop ... --model-dir /tmp/mirage_tuned_eval_v2 --max-iterations 1`) improved overall avg MDAPE from `0.691743` to `0.655222` and improved `fallback_abstain|other` from `0.826780` to `0.694906`, but `structured_boosted|other` still regressed to `0.759270` and protected cohort `sparse_retrieval|fossil|low` regressed to `1.618689`.
- 2026-03-18: Added shared category derivation for workflow dataset SQL + clipboard parsing so dominant `other` families split into `jewel`, `ring`, `amulet`, `belt`, and `map` (while preserving explicit families such as `cluster_jewel`, `essence`, `fossil`, `scarab`, and `logbook`).
- 2026-03-18: Focused verification for category split passed with `.venv/bin/pytest tests/unit/test_ml_workflow_category_derivation.py` (`9 passed`), including checks for representative Crimson/Searching Eye jewel, ring, amulet, belt, and map examples plus a build-dataset SQL assertion.
- 2026-03-18: Hardened mod-feature parsing in `poe_trade/ml/workflows.py` with `_parse_mod_features_json(...)`; both `_feature_dict_from_row(...)` and `_feature_dict_from_parsed_item(...)` now normalize `mod_features_json == ''` and malformed payloads to `{}` instead of raising `JSONDecodeError`.
- 2026-03-18: Added focused regression test `tests/unit/test_ml_workflow_mod_features_json_parsing.py` covering both feature-dict builders for `mod_features_json` values `''` and `'{'`; `.venv/bin/pytest tests/unit/test_ml_workflow_mod_features_json_parsing.py` passed (`2 passed`).
