- 2026-03-18: Kept schema contract unchanged (`ml_item_mod_features_v1` columns untouched) and implemented population in shipped Python workflow layer (`workflows.py`) so both `poe-ml dataset build` and `poeninja_snapshot` service get canonical behavior automatically.
- 2026-03-18: Chose deterministic table refresh (`TRUNCATE TABLE poe_trade.ml_item_mod_features_v1` before repopulation) to keep reruns idempotent and avoid duplicate-version joins from `ReplacingMergeTree` rows during downstream dataset build.
- 2026-03-18: Stopped after measurement-only rebuild because canonical `%tier%` coverage gain versus baseline (`1745` -> `1762`) is too small to qualify as material improvement.
- 2026-03-18: Chose conservative hyperparameter tuning only (estimators/depth/leaf/split/subsample/max_features/learning-rate) inside `_fit_route_bundle_from_aggregates(...)` to target Mirage MDAPE reduction for `fallback_abstain|other` and `structured_boosted|other` before any routing/category-policy changes.
- 2026-03-18: Split hyperparameter policy by route inside `_fit_route_bundle_from_aggregates(...)`: keep higher-capacity defaults for fallback-style routes, but regularize `structured_boosted` and `sparse_retrieval` to reduce overfit on sparse cohorts without altering routing/category decisions.
- 2026-03-18: Implemented category split as a narrow derivation-layer change in `poe_trade/ml/workflows.py` (no schema/migration/routing-policy changes), and mirrored the same heuristics in SQL (`build_dataset`) and Python (`_parse_clipboard_item`) to keep dataset features and prediction parsing coherent.
- 2026-03-18: Implemented a shared workflow-layer parser (`_parse_mod_features_json`) and reused it in both training-row and prediction-item feature-dict builders so invalid or empty `mod_features_json` input is treated as empty feature payload (`{}`) consistently across train/predict paths.
