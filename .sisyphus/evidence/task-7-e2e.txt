E2E Verification Results - 2026-03-17
======================================

Steps 1-5: PASS
- Step 1: Snapshot PoeNinja currency → 88-90 rows ✓
- Step 2: Build FX rates → 91-179 rows ✓  
- Step 3: Normalize prices → 12,889,457 rows (37s) ✓
- Step 4: Build listing events → 0 rows (expected, no event data) ✓
- Step 5: Build dataset → 10,313,522 rows (2m17s) ✓

Step 6: FAIL (ClickHouse HTTP timeout)
- build_comps query takes >300s
- ClickHouse server http_receive_timeout=30s closes connection
- Cannot be overridden via URL params or user profiles
- Server config change causes ClickHouse crash (exit 137)

Root cause: ClickHouse's built-in HTTP timeout (30s) is not configurable
at runtime or via URL params. The setting http_receive_timeout must be in
the main server config XML, but adding it as a top-level element causes
ClickHouse to crash in this version (26.2.4.23).

Workaround: Run build_comps via native TCP protocol or increase server
resources. Alternatively, reduce LIMIT values in build_comps query.
