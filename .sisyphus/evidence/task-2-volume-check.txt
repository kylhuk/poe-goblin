ClickHouse HTTP API queries for v_ps_items_enriched view:

1. Count for Mirage league in last 7 days:
   SELECT count() FROM poe_trade.v_ps_items_enriched WHERE ifNull(league, '') = 'Mirage' AND observed_at >= now() - INTERVAL 7 DAY
   Result: 16041977

2. Total count for Mirage league:
   SELECT count() FROM poe_trade.v_ps_items_enriched WHERE ifNull(league, '') = 'Mirage'
   Result: 16041977

Comparison:
- The 7-day window count equals the total count for the Mirage league.
- This indicates that all data in the view for the Mirage league is within the last 7 days.
- Therefore, the 7-day time boundary does not reduce the scan volume for this league (at least for the current data set).

Note: This may be due to the data being recently ingested or the view only containing recent data.